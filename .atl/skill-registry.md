# Skill Registry — pixel-agents-opencode

Generated by SDD init on 2026-05-16 (re-ran 2026-05-16). All skills are user-level (~/.config/opencode/skills/). No project-level skills found.

## Skills (10 active)

### ⚙️ branch-pr
- **Trigger**: creating, opening, or preparing PRs for review
- **Path**: `~/.config/opencode/skills/branch-pr/SKILL.md`
- **Rules**:
  1. Every PR MUST link an approved issue (Closes/Fixes/Resolves #N)
  2. Every PR MUST have exactly one `type:*` label
  3. Branch names MUST match `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)\/[a-z0-9._-]+$`
  4. Commit messages MUST match conventional commits regex
  5. PR body MUST contain Linked Issue, PR Type, Summary, Changes Table, Test Plan, Contributor Checklist
  6. Automated checks (shellcheck) must pass before merge
  7. No `Co-Authored-By` trailers in commits

### ⚙️ chained-pr
- **Trigger**: PRs over 400 lines, stacked PRs, review slices
- **Path**: `~/.config/opencode/skills/chained-pr/SKILL.md`
- **Rules**:
  1. Split PRs over 400 changed lines unless `size:exception` accepted
  2. Each PR reviewable in ≤60 minutes
  3. Keep tests/docs with the unit they verify
  4. Every child PR must include dependency diagram, marked with 📍
  5. Feature Branch Chain: tracker PR draft/no-merge; children target parent branches
  6. Do not mix chain strategies after user chooses one
  7. Verify each PR independently (CI/tests/docs/diff)

### 📖 cognitive-doc-design
- **Trigger**: writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs
- **Path**: `~/.config/opencode/skills/cognitive-doc-design/SKILL.md`
- **Rules**:
  1. Lead with the answer — context comes after
  2. Progressive disclosure: happy path first, then edge cases
  3. Chunk related info into small sections; keep lists short
  4. Use headings, labels, callouts for signposting
  5. Prefer tables, checklists, examples over prose (recognition over recall)
  6. Design docs so reviewers don't need to reconstruct intent
  7. PR docs: state review order, out-of-scope, link prev/next PR in chains

### 💬 comment-writer
- **Trigger**: PR feedback, issue replies, reviews, Slack messages, or GitHub comments
- **Path**: `~/.config/opencode/skills/comment-writer/SKILL.md`
- **Rules**:
  1. Start with the actionable point, not recap
  2. Be warm and direct — thoughtful teammate, not corporate bot
  3. Keep it short: 1-3 paragraphs or tight bullet list
  4. Explain why when asking for a change
  5. Comment on highest-value issue, not every preference
  6. Match thread language; Spanish → Rioplatense/voseo
  7. No em dashes — use commas, periods, parentheses

### 🏛️ experto-arquitectura-ddd
- **Trigger**: DDD, hexagonal architecture, ports & adapters, bounded contexts
- **Path**: `~/.config/opencode/skills/experto-arquitectura-ddd/SKILL.md`
- **Rules**:
  1. Core domain never knows about infrastructure or frameworks
  2. All external access through a Port (interface)
  3. Adapters implement Ports, never reverse
  4. Entities have identity; Value Objects do not
  5. Aggregate Root controls access to child entities
  6. Domain Events for cross-context communication
  7. Don't inject repositories into domain services — use ports
  8. Ubiquitous Language in code (same as business language)

### 🧪 experto-tdd
- **Trigger**: writing tests, TDD red-green-refactor cycle, testing principles
- **Path**: `~/.config/opencode/skills/experto-tdd/SKILL.md`
- **Rules**:
  1. ALWAYS write the test first
  2. Test must fail (red) before writing production code
  3. One assertion/concept per test
  4. Test names as specification: testGivenXWhenYThenZ
  5. Every test follows Arrange-Act-Assert
  6. Tests must be independent (any order)
  7. Refactor after green, not before

### 🐹 go-testing
- **Trigger**: Go tests, go test coverage, Bubbletea teatest, golden files
- **Path**: `~/.config/opencode/skills/go-testing/SKILL.md`
- **Rules**:
  1. Prefer table-driven tests with `t.Run(tt.name, ...)`
  2. Test behavior and state transitions, not implementation trivia
  3. Use `t.TempDir()` for filesystem tests
  4. Integration tests skippable with `testing.Short()`
  5. Golden files must be deterministic; update via `-update` path
- **⚠️ Not applicable to this TypeScript/Bun project**

### 📝 issue-creation
- **Trigger**: creating GitHub issues, bug reports, or feature requests
- **Path**: `~/.config/opencode/skills/issue-creation/SKILL.md`
- **Rules**:
  1. Blank issues disabled — MUST use templates (bug/feature)
  2. Auto-labeled `status:needs-review` on creation
  3. Maintainer MUST add `status:approved` before any PR
  4. Questions → Discussions, NOT issues
  5. Pre-flight checks required (no duplicate + understands approval workflow)

### ⚖️ judgment-day
- **Trigger**: judgment day, dual review, adversarial review, juzgar
- **Path**: `~/.config/opencode/skills/judgment-day/SKILL.md`
- **Rules**:
  1. Two blind judges in parallel; never review yourself
  2. Wait for both before synthesis
  3. Classify warnings: real (normal use triggers it) vs theoretical (downgrade to INFO)
  4. Ask before fixing Round 1 confirmed issues
  5. Re-judge immediately after fixes
  6. Max 2 fix iterations with remaining issues; ask user to continue
  7. Terminal states only: JUDGMENT: APPROVED or JUDGMENT: ESCALATED

### 🛠️ skill-creator
- **Trigger**: new skills, agent instructions, documenting AI usage patterns
- **Path**: `~/.config/opencode/skills/skill-creator/SKILL.md`
- **Rules**:
  1. Skills are runtime instruction contracts for LLMs, not human docs
  2. `description` MUST be one physical line, quoted, ≤250 chars, trigger-first
  3. Body target: 180-450 tokens; hard max: 1000 tokens
  4. Required structure: Activation Contract → Hard Rules → Decision Gates → Execution Steps → Output Contract → References
  5. Supporting material → `assets/` or `references/`, not body
  6. Frontmatter MUST include name, description, license, metadata.author, metadata.version
  7. Check `docs/skill-style-guide.md` before inline fallback rules

### 📦 work-unit-commits
- **Trigger**: implementation, commit splitting, chained PRs, keeping tests and docs with code
- **Path**: `~/.config/opencode/skills/work-unit-commits/SKILL.md`
- **Rules**:
  1. Commit by work unit, not by file type
  2. Keep tests with the code they verify (same commit)
  3. Keep docs with user-visible changes
  4. Tell a story — reviewer should understand "why" from diff + message
  5. Each commit should be a candidate chained PR
  6. SDD workload guard: if >400-line forecast, group commits into chained PR slices
  7. Rollback must be reasonable without reverting unrelated work
